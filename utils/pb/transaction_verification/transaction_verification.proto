syntax = "proto3";

package hello;

service VerificationService {
    rpc VerificationLogic (VerificationRequest) returns (VerificationResponse);
}

message VerificationRequest {
    int32 itemsLength = 1;
}

message VerificationResponse {
    string verdict = 1;
}